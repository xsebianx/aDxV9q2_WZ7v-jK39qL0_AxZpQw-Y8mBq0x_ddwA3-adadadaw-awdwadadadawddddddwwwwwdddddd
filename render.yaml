services:
  - type: web
    name: ticket-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python exyliomc.py
    volumeMounts:
      - name: ticket-data
        mountPath: /data

volumes:
  - name: ticket-data
    plan: free
