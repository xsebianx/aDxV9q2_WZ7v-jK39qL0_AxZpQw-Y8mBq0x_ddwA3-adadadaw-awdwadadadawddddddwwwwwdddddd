from flask import Flask
from threading import Thread

app = Flask(__name__)

@app.route('/')  # Ruta principal
def home():
    return "🟢 Bot ExilioMC está en línea!"  # Mensaje que verás al entrar al link

def run():
    app.run(host='0.0.0.0', port=10000)  # Render usa automáticamente el puerto 10000

Thread(target=run).start()  # Inicia Flask en segundo plano